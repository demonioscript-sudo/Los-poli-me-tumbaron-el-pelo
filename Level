-- [[ PARRA NINJA HUB MASHMELLOW - PARRABDOMINA ]]
-- ESTILO ACTUALIZADO: DARK PREMIUM MORADO/NEGRO + LETRAS NEON

local _K = "PARRAHUD"
local lp = game:GetService("Players").LocalPlayer
local vim = game:GetService("VirtualInputManager")
local vu = game:GetService("VirtualUser")
local coreGui = game:GetService("CoreGui")
local http = game:GetService("HttpService")

-- Seguridad
local targetParent = lp:FindFirstChild("PlayerGui") or coreGui
local function CleanUI()
    for _, v in pairs(targetParent:GetChildren()) do
        if v:FindFirstChild("AC_TAG_DOMINA CON PARRA") then v:Destroy() end
    end
end
CleanUI()

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RobloxGui"
ScreenGui.Parent = targetParent
ScreenGui.ResetOnSpawn = false

local tag = Instance.new("BoolValue", ScreenGui)
tag.Name = "AC_TAG_DOMINA CON PARRA"

-- === NUEVO ESTILO DARK PREMIUM MORADO ===
local MainPurple = Color3.fromRGB(170, 0, 255)     -- Morado principal
local NeonPurple = Color3.fromRGB(200, 100, 255)   -- Neon morado
local DarkBg = Color3.fromRGB(8, 8, 12)            -- Negro muy oscuro
local DarkerBg = Color3.fromRGB(5, 5, 8)

-- [ LOGIN: PARRA DOMINA - ESTILO PREMIUM ]
local LoginFrame = Instance.new("Frame", ScreenGui)
LoginFrame.Size = UDim2.new(0, 320, 0, 220)
LoginFrame.Position = UDim2.new(0.5, -160, 0.4, -110)
LoginFrame.BackgroundColor3 = DarkBg
Instance.new("UICorner", LoginFrame).CornerRadius = UDim.new(0, 12)

local LStroke = Instance.new("UIStroke", LoginFrame)
LStroke.Color = MainPurple
LStroke.Thickness = 2.5
LStroke.Transparency = 0.1

local LTitle = Instance.new("TextLabel", LoginFrame)
LTitle.Size = UDim2.new(1, 0, 0, 50)
LTitle.Text = "PARRA DOMINA"
LTitle.TextColor3 = NeonPurple
LTitle.Font = Enum.Font.GothamBlack
LTitle.TextSize = 24
LTitle.BackgroundTransparency = 1
LTitle.TextStrokeTransparency = 0.8
LTitle.TextStrokeColor3 = MainPurple

local KeyInput = Instance.new("TextBox", LoginFrame)
KeyInput.Size = UDim2.new(0.85, 0, 0, 45)
KeyInput.Position = UDim2.new(0.075, 0, 0.38, 0)
KeyInput.PlaceholderText = "ENTER KEY"
KeyInput.BackgroundColor3 = DarkerBg
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderColor3 = Color3.fromRGB(120, 80, 180)
KeyInput.Font = Enum.Font.GothamSemibold
KeyInput.TextSize = 16
local KeyCorner = Instance.new("UICorner", KeyInput)
KeyCorner.CornerRadius = UDim.new(0, 10)
local KeyStroke = Instance.new("UIStroke", KeyInput)
KeyStroke.Color = MainPurple
KeyStroke.Thickness = 1.5

local LoginBtn = Instance.new("TextButton", LoginFrame)
LoginBtn.Size = UDim2.new(0.85, 0, 0, 45)
LoginBtn.Position = UDim2.new(0.075, 0, 0.68, 0)
LoginBtn.BackgroundColor3 = MainPurple
LoginBtn.Text = "VERIFICAR"
LoginBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
LoginBtn.Font = Enum.Font.GothamBold
LoginBtn.TextSize = 18
Instance.new("UICorner", LoginBtn).CornerRadius = UDim.new(0, 10)

-- [ HUB CONTROL - ESTILO DARK PREMIUM MORADO ]
local function StartHub()
    LoginFrame:Destroy()
    
    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Size = UDim2.new(0, 480, 0, 380)
    MainFrame.Position = UDim2.new(0.5, -240, 0.5, -190)
    MainFrame.BackgroundColor3 = DarkBg
    MainFrame.Active = true
    MainFrame.Draggable = true
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

    local MainStroke = Instance.new("UIStroke", MainFrame)
    MainStroke.Color = MainPurple
    MainStroke.Thickness = 2.8

    -- Botón ABRIR (minimizar)
    local OpenBtn = Instance.new("TextButton", ScreenGui)
    OpenBtn.Size = UDim2.new(0, 90, 0, 35)
    OpenBtn.Position = UDim2.new(0, 15, 0.5, 0)
    OpenBtn.BackgroundColor3 = DarkBg
    OpenBtn.Text = "PARRA HUB"
    OpenBtn.TextColor3 = NeonPurple
    OpenBtn.Visible = false
    OpenBtn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(0, 8)
    Instance.new("UIStroke", OpenBtn).Color = MainPurple

    local Title = Instance.new("TextLabel", MainFrame)
    Title.Size = UDim2.new(1, 0, 0, 50)
    Title.Text = "PARRA NINJA HUB MASHMELLOW"
    Title.TextColor3 = NeonPurple
    Title.Font = Enum.Font.GothamBlack
    Title.TextSize = 22
    Title.BackgroundTransparency = 1
    Title.TextStrokeTransparency = 0.85
    Title.TextStrokeColor3 = MainPurple

    -- Botones X y -
    local CloseBtn = Instance.new("TextButton", MainFrame)
    CloseBtn.Size = UDim2.new(0, 32, 0, 32)
    CloseBtn.Position = UDim2.new(1, -38, 0, 8)
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 20

    local MiniBtn = Instance.new("TextButton", MainFrame)
    MiniBtn.Size = UDim2.new(0, 32, 0, 32)
    MiniBtn.Position = UDim2.new(1, -72, 0, 8)
    MiniBtn.BackgroundTransparency = 1
    MiniBtn.Text = "−"
    MiniBtn.TextColor3 = NeonPurple
    MiniBtn.Font = Enum.Font.GothamBold
    MiniBtn.TextSize = 24

    -- Info Label
    local InfoLabel = Instance.new("TextLabel", MainFrame)
    InfoLabel.Size = UDim2.new(0.52, 0, 0, 280)
    InfoLabel.Position = UDim2.new(0.04, 0, 0, 70)
    InfoLabel.BackgroundColor3 = DarkerBg
    InfoLabel.TextColor3 = Color3.fromRGB(220, 220, 255)
    InfoLabel.Font = Enum.Font.Code
    InfoLabel.TextSize = 13
    InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
    InfoLabel.TextYAlignment = Enum.TextYAlignment.Top
    Instance.new("UICorner", InfoLabel).CornerRadius = UDim.new(0, 10)
    Instance.new("UIStroke", InfoLabel).Color = MainPurple

    local function CreateBtn(txt, pos)
        local b = Instance.new("TextButton", MainFrame)
        b.Size = UDim2.new(0.36, 0, 0, 55)
        b.Position = pos
        b.BackgroundColor3 = Color3.fromRGB(25, 10, 40)
        b.TextColor3 = NeonPurple
        b.Font = Enum.Font.GothamBold
        b.TextSize = 15
        b.Text = txt
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 10)
        local stroke = Instance.new("UIStroke", b)
        stroke.Color = MainPurple
        stroke.Thickness = 1.8
        return b
    end

    local FarmBtn = CreateBtn("FARM MASHMELLOW", UDim2.new(0.60, 0, 0, 75))
    local SellBtn = CreateBtn("SELL MASHMELLOW", UDim2.new(0.60, 0, 0, 145))
    local ToolBtn = CreateBtn("MASHMELLOW TOOL", UDim2.new(0.60, 0, 0, 215))

    local farmActive, sellActive = false, false

    -- === LÓGICA ORIGINAL (sin tocar) ===
    local items = {"Water", "Sugar Block Bag", "Gelatin", "Empty Bag"}

    CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)
    MiniBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false; OpenBtn.Visible = true end)
    OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = true; OpenBtn.Visible = false end)

    FarmBtn.MouseButton1Click:Connect(function()
        farmActive = not farmActive
        FarmBtn.Text = farmActive and "STOP FARM" or "FARM MASHMELLOW"
        FarmBtn.BackgroundColor3 = farmActive and Color3.fromRGB(80, 0, 120) or Color3.fromRGB(25, 10, 40)
    end)

    SellBtn.MouseButton1Click:Connect(function()
        sellActive = not sellActive
        SellBtn.Text = sellActive and "AUTO-SELL ON" or "SELL MASHMELLOW"
        SellBtn.BackgroundColor3 = sellActive and Color3.fromRGB(80, 0, 120) or Color3.fromRGB(25, 10, 40)
    end)

    ToolBtn.MouseButton1Click:Connect(function()
        local t = Instance.new("Tool", lp.Backpack)
        t.Name = "MASHMELLOW DELETE"
        t.RequiresHandle = false
        t.Activated:Connect(function()
            local m = lp:GetMouse()
            if m.Target then pcall(function() m.Target:Destroy() end) end
        end)
    end)

    -- Loop de conteo e info (mantengo la lógica original)
    task.spawn(function()
        while task.wait(1) do
            local char = lp.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then continue end

            if sellActive then
                -- (lógica de auto-sell sin cambios)
                local isNear = false
                for _, v in pairs(workspace:GetDescendants()) do
                    if (v.Name:lower():find("sell") or v.Name:lower():find("buyer") or v:IsA("ProximityPrompt")) then
                        local pos = v:IsA("Model") and v.PrimaryPart and v.PrimaryPart.Position or (v:IsA("BasePart") and v.Position) or nil
                        if pos and (char.HumanoidRootPart.Position - pos).Magnitude < 15 then
                            isNear = true
                            break
                        end
                    end
                end
                if isNear then
                    pcall(function()
                        for _, i in pairs(lp.Backpack:GetChildren()) do
                            if i.Name:find("Small") or i.Name:find("Medium") or i.Name:find("Large") then
                                char.Humanoid:EquipTool(i)
                                task.wait(0.2)
                                vim:SendKeyEvent(true, Enum.KeyCode.E, false, game)
                                task.wait(0.1)
                            end
                        end
                    end)
                end
            end

            local c = {W=0, S=0, G=0, B=0, Sm=0, M=0, L=0}
            local inv = lp.Backpack:GetChildren()
            for _, v in pairs(char:GetChildren()) do table.insert(inv, v) end
            for _, i in pairs(inv) do
                if i.Name == "Water" then c.W += 1
                elseif i.Name == "Sugar Block Bag" then c.S += 1
                elseif i.Name == "Gelatin" then c.G += 1
                elseif i.Name == "Empty Bag" then c.B += 1
                elseif i.Name:find("Small") then c.Sm += 1
                elseif i.Name:find("Medium") then c.M += 1
                elseif i.Name:find("Large") then c.L += 1 end
            end

            InfoLabel.Text = string.format([[
   PARRA NINJA HUB
   ═══════════════════
   
   PRODUCTOS:
   • Small:    %d
   • Medium:   %d
   • Large:    %d

   MATERIALES:
   • Agua:     %d
   • Azúcar:   %d
   • Gelatina: %d
   • Bolsas:   %d

   ═══════════════════
   STATUS: ANTI-BAN ACTIVE
   KEY: VERIFIED
            ]], c.Sm, c.M, c.L, c.W, c.S, c.G, c.B)
        end
    end)
end

-- Login
LoginBtn.MouseButton1Click:Connect(function()
    if KeyInput.Text == _K then 
        StartHub() 
    else 
        LoginBtn.Text = "KEY INCORRECTA"
        task.wait(1.5)
        LoginBtn.Text = "VERIFICAR"
    end
end)

lp.Idled:Connect(function() 
    vu:CaptureController() 
    vu:ClickButton2(Vector2.new()) 
end)
